# Line endings are not cosmetic here. A CRLF in a shell script produces
# "bad interpreter: /bin/bash^M" on a Linux host, and this repo is checked out
# on Windows and run on Linux. Normalise everything to LF.
* text=auto eol=lf

# Binary files git should never touch.
*.png binary
*.jpg binary
*.gz  binary
*.pdf binary
