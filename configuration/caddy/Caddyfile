# Do not edit this file.
# It simply loads the mode you chose with TLS_MODE in the .env file.
# The four modes live in configuration/caddy/modes/ and are short enough to read.

{
	admin off
}

import /etc/caddy/modes/{$TLS_MODE}.caddy
